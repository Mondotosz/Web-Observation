

var url_string = window.location.href

var url = new URL(url_string);

var search = url.searchParams.get("error=");

console.log(c);

switch (url) {
    case 1:

        break;
    case 1:

        break;
    case 1:

        break;
    case 1:

        break;
    case 1:

        break;
    case 1:

        break;
    default:

        break;
}